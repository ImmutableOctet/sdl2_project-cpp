#pragma once

#include "opengl.h"

// Namespace(s):
namespace game
{
	namespace graphics
	{
		using shaderHandle = GLuint;
		using shaderLocation = GLint;
		using textureHandle = GLuint;
	}
}