// Includes:
#include "camera.h"

// Namespaces:
namespace game
{
	namespace graphics
	{
		camera::camera(const glm::vec3& pos, const glm::vec3& rot) : position(pos), rotation(rot)
		{
			// Nothing so far.
		}
	}
}